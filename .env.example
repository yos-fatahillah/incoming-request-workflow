#   export ANTHROPIC_API_KEY=sk-ant-...
#
# If this is left unset, the prototype automatically falls back to a local
# keyword-based classifier/drafter so it remains fully demoable offline.
ANTHROPIC_API_KEY=
